[project]
name = "prometheus"
ecosystem = "go-modules"
canonical_id = "go-modules:github.com/prometheus/prometheus"
description = "Prometheus — open-source monitoring and alerting system"
why_included = "Top-25 Go modules; TLS for scraping targets; remote_write encryption; bearer token auth; representative of cloud-native monitoring stack"

[repo]
url = "https://github.com/prometheus/prometheus"
# Re-pinned 2026-08-29. The original pin 121eaa177c0f6d7a13b53c77a5ab51f14a4baa3a is not a
# commit in the repository this project clones, so `clone_all.sh` could never
# check it out: it clones with --no-checkout, the checkout failed, and the
# project was left with an empty working tree that every corpus figure counted
# as "scanned, zero findings". Ten of the unreachable pins are the pinned or
# current sha of a different project in this same corpus, so the pins were
# shuffled across project files at construction rather than lost since.
commit_sha = "5c816f70b15e480b028b80f281cfa414f5639153"
license = "Apache-2.0"
language_primary = "go"
languages_secondary = []

[metadata]
downloads_monthly = 4000000
captured_at = "2026-06-15"
stars = 55000
last_commit_date = "2026-06-14"

[selection]
selection_method = "top-25-go-module-downloads"
selection_rank = 15
substituted_for = ""

[scan_hints]
scan_paths = ["config/", "web/"]
exclude_paths = ["vendor/"]
expected_languages = ["go"]
