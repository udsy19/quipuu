[project]
name = "go-redis"
ecosystem = "go-modules"
canonical_id = "go-modules:github.com/redis/go-redis"
description = "go-redis — Redis client for Go with TLS support"
why_included = "Top-25 Go modules; TLS for Redis connections; AUTH command; ACL support; widely used in Go backends requiring secure cache connections"

[repo]
url = "https://github.com/redis/go-redis"
# Re-pinned 2026-08-29. The original pin bf57a51fd879fbd9b4b4f79f26ae55ccc9fef3d1 is not a
# commit in the repository this project clones, so `clone_all.sh` could never
# check it out: it clones with --no-checkout, the checkout failed, and the
# project was left with an empty working tree that every corpus figure counted
# as "scanned, zero findings". Ten of the unreachable pins are the pinned or
# current sha of a different project in this same corpus, so the pins were
# shuffled across project files at construction rather than lost since.
commit_sha = "18837034a1b96d331567d4fea23b303fd8bf2800"
license = "BSD-2-Clause"
language_primary = "go"
languages_secondary = []

[metadata]
downloads_monthly = 7000000
captured_at = "2026-06-15"
stars = 20000
last_commit_date = "2026-06-12"

[selection]
selection_method = "top-25-go-module-downloads"
selection_rank = 22
substituted_for = ""

[scan_hints]
scan_paths = []
exclude_paths = []
expected_languages = ["go"]
