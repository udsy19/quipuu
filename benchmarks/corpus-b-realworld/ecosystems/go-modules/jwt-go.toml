[project]
name = "jwt-go"
ecosystem = "go-modules"
canonical_id = "go-modules:github.com/dgrijalva/jwt-go"
description = "Original Go JWT library (archived) — predecessor to golang-jwt/jwt"
why_included = "Still heavily downloaded; large legacy install base; HS/RS/ES algorithm usage; needed for benchmark coverage of unmaintained-but-prevalent code"

[repo]
url = "https://github.com/dgrijalva/jwt-go"
# Re-pinned 2026-08-29. The original pin 9742bd7f95cf26d5e6a28e45b26e99ffdfe2b3f1 is not a
# commit in the repository this project clones, so `clone_all.sh` could never
# check it out: it clones with --no-checkout, the checkout failed, and the
# project was left with an empty working tree that every corpus figure counted
# as "scanned, zero findings". Ten of the unreachable pins are the pinned or
# current sha of a different project in this same corpus, so the pins were
# shuffled across project files at construction rather than lost since.
commit_sha = "9742bd7fca1c67ba2eb793750f56ee3094d1b04f"
license = "MIT"
language_primary = "go"
languages_secondary = []

[metadata]
downloads_monthly = 9000000
captured_at = "2026-06-15"
stars = 10500
last_commit_date = "2021-11-09"

[selection]
selection_method = "top-25-go-module-downloads"
selection_rank = 7
substituted_for = ""

[scan_hints]
scan_paths = []
exclude_paths = ["test/"]
expected_languages = ["go"]
