[project]
name = "age"
ecosystem = "crates-io"
canonical_id = "crates-io:age"
description = "age file encryption library in Rust"
why_included = "Top-30 crates.io; X25519/Ed25519/scrypt encryption format; ChaCha20-Poly1305/AES-GCM; modern file encryption with quantum-vulnerable key agreement"

[repo]
url = "https://github.com/str4d/age"
commit_sha = "9fdb1256415935a4425b6b607ea843ff85b854ae"
license = "MIT OR Apache-2.0"
language_primary = "rust"
languages_secondary = []

[metadata]
downloads_monthly = 1500000
captured_at = "2026-06-15"
stars = 2300
last_commit_date = "2026-04-01"

[selection]
selection_method = "top-25-crates-io-downloads"
selection_rank = 23
substituted_for = ""

[scan_hints]
# Repaired 2026-08-29. This crates-io entry clones github.com/str4d/age, which
# is a Go repository (cmd/, internal/, go.mod) and has no age/src/ and no Rust.
# The Rust crate of this name lives at str4d/rage. Recorded rather than
# silently re-pointed: changing the URL changes what the corpus contains and
# needs a re-clone. Scope is the whole repository, which is what it was
# already being scanned as.
scan_paths = []
exclude_paths = ["tests/"]
expected_languages = ["rust"]
