[project]
name = "oqs-provider"
ecosystem = "crypto-adjacent"
canonical_id = "crypto-adjacent:github.com/open-quantum-safe/oqs-provider"
description = "OQS-Provider — OpenSSL 3.x provider integrating liboqs PQC algorithms"
why_included = "OpenSSL 3.x PQC provider; ML-KEM/ML-DSA/SLH-DSA via OpenSSL EVP API; production path for PQC in existing OpenSSL deployments; benchmark must detect provider-based PQC usage"

[repo]
url = "https://github.com/open-quantum-safe/oqs-provider"
# Re-pinned 2026-08-29. The original pin 35eceb69d2b363cb0421085cf1ae1c682dee1acc is not a
# commit in the repository this project clones, so `clone_all.sh` could never
# check it out: it clones with --no-checkout, the checkout failed, and the
# project was left with an empty working tree that every corpus figure counted
# as "scanned, zero findings". Ten of the unreachable pins are the pinned or
# current sha of a different project in this same corpus, so the pins were
# shuffled across project files at construction rather than lost since.
commit_sha = "72e7dae5aab022167aa7ae8f5afdb9667d848104"
license = "MIT"
language_primary = "c"
languages_secondary = []

[metadata]
downloads_monthly = 100000
captured_at = "2026-06-15"
stars = 600
last_commit_date = "2026-06-10"

[selection]
selection_method = "curated-crypto-adjacent"
selection_rank = 11
substituted_for = ""

[scan_hints]
scan_paths = ["oqsprov/"]
exclude_paths = ["test/"]
expected_languages = ["c"]
