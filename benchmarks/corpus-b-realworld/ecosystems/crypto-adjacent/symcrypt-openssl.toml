[project]
name = "symcrypt-openssl"
ecosystem = "crypto-adjacent"
canonical_id = "crypto-adjacent:github.com/microsoft/SymCrypt-OpenSSL"
description = "SymCrypt-OpenSSL — OpenSSL 3.x provider backed by Microsoft SymCrypt"
why_included = "OpenSSL 3.x provider using SymCrypt; PQC algorithms via SymCrypt backend; used in Azure Linux; benchmark tests provider architecture for enterprise Linux PQC deployment"

[repo]
url = "https://github.com/microsoft/SymCrypt-OpenSSL"
# Re-pinned 2026-08-29. The original pin d35ba3fe5449bee3e6d43e1f296c3ca818bd36be is not a
# commit in the repository this project clones, so `clone_all.sh` could never
# check it out: it clones with --no-checkout, the checkout failed, and the
# project was left with an empty working tree that every corpus figure counted
# as "scanned, zero findings". Ten of the unreachable pins are the pinned or
# current sha of a different project in this same corpus, so the pins were
# shuffled across project files at construction rather than lost since.
commit_sha = "d73b1e9ebf589f5c0098810e74e9556b2bef776a"
license = "MIT"
language_primary = "c"
languages_secondary = []

[metadata]
downloads_monthly = 100000
captured_at = "2026-06-15"
stars = 350
last_commit_date = "2026-06-10"

[selection]
selection_method = "curated-crypto-adjacent"
selection_rank = 16
substituted_for = ""

[scan_hints]
scan_paths = ["SymCryptProvider/src/"]
exclude_paths = []
expected_languages = ["c"]
