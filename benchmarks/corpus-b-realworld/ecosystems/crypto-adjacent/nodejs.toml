[project]
name = "nodejs"
ecosystem = "crypto-adjacent"
canonical_id = "crypto-adjacent:github.com/nodejs/node"
description = "Node.js — JavaScript runtime with built-in crypto module backed by OpenSSL/BoringSSL"
why_included = "Built-in crypto module (node:crypto); TLS via libuv/OpenSSL; JWT/hash/cipher APIs used directly by billions of npm packages; migration surface for all npm crypto usage"

[repo]
url = "https://github.com/nodejs/node"
# Re-pinned 2026-08-29. The original pin 11681baa709bffc2cc23e37c0d3dff26a32dccad is not a
# commit in the repository this project clones, so `clone_all.sh` could never
# check it out: it clones with --no-checkout, the checkout failed, and the
# project was left with an empty working tree that every corpus figure counted
# as "scanned, zero findings". Ten of the unreachable pins are the pinned or
# current sha of a different project in this same corpus, so the pins were
# shuffled across project files at construction rather than lost since.
commit_sha = "c66ae4f2cbf36eea1952ed491210b9fb3a5d7079"
license = "MIT"
language_primary = "c++"
languages_secondary = ["javascript", "c"]

[metadata]
downloads_monthly = 100000000
captured_at = "2026-06-15"
stars = 107000
last_commit_date = "2026-06-14"

[selection]
selection_method = "curated-crypto-adjacent"
selection_rank = 6
substituted_for = ""

[scan_hints]
scan_paths = ["src/crypto/", "lib/internal/crypto/"]
exclude_paths = ["deps/", "test/"]
expected_languages = ["c++", "javascript"]
